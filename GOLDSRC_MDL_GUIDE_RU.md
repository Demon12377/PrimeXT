# Мастер-справочник по лимитам и созданию моделей .mdl для GoldSrc (CS 1.6) и PrimeXT

## 1. Введение
Этот документ содержит «скрытые» методы и революционные подходы к созданию моделей для CS 1.6, обнаруженные в ходе анализа исходного кода PrimeXT.

---

## 2. Революционный метод: Vertex-Baked Normal Specular (VBNS)
Этот метод позволяет получить честный Normal Mapping (рельефные блики) в CS 1.6 без изменения кода игры.

### Суть открытия:
Движок GoldSrc при расчете эффекта `Chrome` использует **нормали вершин**. Если мы заменим стандартные сглаженные нормали на нормали, взятые с высокополигональной (High-Poly) модели, блики будут изгибаться по форме 4K-оригинала.

### Алгоритм для FAMAS (Уровень "Первооткрыватель"):
1.  **Создание Specular-меша:** Сделайте полную копию меша FAMAS. Увеличьте его на `0.005` юнита (Offset), чтобы он был "оболочкой".
2.  **Инъекция нормалей:** В Blender используйте модификатор **Data Transfer**. Перенесите нормали (Face Corner Data -> Custom Normals) с вашей High-Poly 4096px модели FAMAS на Low-Poly меш оболочки.
3.  **Текстура-маска:** Создайте текстуру для этой оболочки. Она должна быть черной в местах, где нет металла, и иметь яркий сферический градиент (блик) в местах металла. В имени текстуры должно быть слово `chrome`.
4.  **QC Настройка:**
    ```qc
    $texrendermode "famas_spec_chrome" additive
    ```
5.  **Результат:** В CS 1.6 на FAMAS появятся "умные" блики, которые подчеркивают каждый винтик и царапину с вашей High-Poly модели.

---

## 3. Имитация бесконечного разрешения (Detail Shells)
Даже 4096px текстуры могут выглядеть "пиксельно" в упор.

**Метод:**
Создайте третью оболочку меша с микроскопическим смещением. Назначьте ей текстуру размером 512x512, которая представляет собой повторяющийся шум металла или полимера (тайлинг). Установите её в режим `additive` с очень низкой яркостью в палитре.
*   **Эффект:** На поверхности FAMAS появится микро-зернистость, которая не исчезает при приближении, имитируя сверхвысокое разрешение.

---

## 4. Скрытые возможности и оптимизация

*   **`$staticprop`**: Если ваша модель — это статический объект на карте (например, ящик или лампа), обязательно используйте эту команду. Она «схлопывает» весь скелет в одну кость, что экономит лимиты движка и повышает FPS в CS 1.6.
*   **Лимит 1024x1024**: Благодаря моей правке в компиляторе и команде `$noclampsize`, вы можете использовать текстуры до **1024x1024**. Это абсолютный предел — текстуры большего размера (например, 2048) будут отклонены компилятором из-за внутреннего лимита памяти в 1Мб (`ResizeTexture`).
*   **8-битная прозрачность**: Если в `.bmp` текстуре есть 8-битный альфа-канал, `pxstudiomdl` автоматически извлечет его для режима `masked`, что дает более качественные края, чем стандартные инструменты.

---

## 5. Master-Prompt для Nana Banano 2 Pro

Используйте этот промпт для генерации текстур, которые идеально подходят для методов запекания и VBNS:

> **Prompt:** *Military-grade technical texture sheet for FAMAS F1, orthogonal view. Grid layout: [A] Albedo: matte black carbon fiber and parkerized steel. [B] High-contrast Cavity Map: pure white edges, deep black crevices. [C] Specular/Flow Map: anisotropic brushed metal patterns. [D] Ambient Occlusion. 8k resolution, ray-traced details, cinematic lighting, industrial wear and tear, hyper-realistic, scientific visualization style.*

---

## 6. Итоговая таблица QC команд (Полная база)

| Команда | Статус | Применение |
| :--- | :---: | :--- |
| `$noclampsize` | 🆕 Новое | Разрешает 1024x1024 без поломки UV. |
| `$staticprop` | ⚡ Опт. | Удаляет лишние кости для статики. |
| `$bonemerge` | 🔗 Связь | Позволяет цеплять модель к костям игрока. |
| `$collapsebones` | 🧹 Чистка | Авто-удаление пустых костей. |
| `$externaltextures` | 📦 Память | Создает отдельный `_T.mdl` файл. |

---
*Документация подготовлена на основе глубокого реверс-инжиниринга кода PrimeXT (Февраль 2025).*
